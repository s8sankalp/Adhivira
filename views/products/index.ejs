<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nidar Naari - Women Safety Products</title>
    <style>
        /* Global Styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #fff 30%, #f3f4f6 100%);
            color: #333;
            line-height: 1.6;
            overflow-x: hidden;
            background-image: url('https://plus.unsplash.com/premium_photo-1668473366952-45f06fbf6492?w=600&auto=format&fit=crop&q=60&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxzZWFyY2h8OXx8d2Vic2l0ZSUyMGJhY2tncm91bmR8ZW58MHx8MHx8fDA%3D');
            background-size: cover;
            background-repeat: no-repeat;
            background-position: center;
            background-attachment: fixed;
        }

        /* Header Styles */
        header {
            background-color: #ff6f61;
            color: white;
            padding: 20px 0;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 100;
        }

        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        .navbar h1 {
            font-size: 24px;
            font-weight: 600;
        }

        .navbar nav ul {
            list-style: none;
            display: flex;
        }

        .navbar nav ul li {
            margin-left: 30px;
        }

        .navbar nav ul li a {
            color: white;
            text-decoration: none;
            font-weight: 500;
            transition: color 0.3s ease;
        }

        .navbar nav ul li a:hover {
            color: #ffe600; /* Gold hover effect */
        }

        /* Main Content Styles */
        main {
            max-width: 1200px;
            margin: 100px auto 40px; /* Adjust for fixed header */
            padding: 20px;
        }

        .card {
            background-color: white;
            padding: 30px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
            border-radius: 15px;
            margin-bottom: 30px;
        }

        .product-card {
            display: flex; /* Use flexbox for layout */
            align-items: center; /* Center items vertically */
            background-color: #f3f4f6;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
            margin-bottom: 20px; /* Space between product cards */
        }

        .product-card img {
            width: 100px; /* Set a fixed width for the image */
            height: auto; /* Maintain aspect ratio */
            border-radius: 10px; /* Rounded corners for images */
            margin-left: 20px; /* Space between text and image */
        }

        .product-card h3 {
            font-size: 18px;
            margin-bottom: 10px;
        }

        .product-card p {
            margin-bottom: 10px;
        }

        .product-card button {
            background-color: #ff6f61;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            margin-right: 10px; /* Space between buttons */
        }

        .product-card button:hover {
            background-color: #ff4f50;
        }

        /* Footer Styles */
        footer {
            text-align: center;
            padding: 20px;
            background-color: #333;
            color: white;
            position: relative;
            bottom: 0;
            width: 100%;
        }

        footer p {
            margin: 0;
            font-size: 14px;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .navbar nav ul {
                flex-direction: column;
                text-align: center;
            }

            .navbar nav ul li {
                margin: 10px 0;
            }

            .product-card {
                flex-direction: column; /* Stack on smaller screens */
                align-items: flex-start; /* Align items to the start */
            }

            .product-card img {
                margin: 10px 0 0 0; /* Adjust margin for stacked layout */
            }
        }
    </style>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
</head>
<body>
    <header>
        <div class="navbar">
            <h1>Nidar Naari</h1>
            <nav>
                <ul>
                    <li><a href="/">Home</a></li>
                    <li><a href="/safety">Safety Tips</a></li>
                    <li><a href="/contact">Contact</a></li>
                </ul>
            </nav>
        </div>
    </header>

    <main>
        <section id="product-list-section" class="animated-section fadeIn">
            <div class="card animated-card">
                <h2>Women Safety Products</h2>
                <div id="product-list"></div> <!-- Dynamic products inserted via JavaScript -->
            </div>
        </section>
    </main>

    <footer>
        <p>© 2024 Women Safety App | All Rights Reserved</p>
    </footer>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            // Dummy product data (replace with actual backend data)
            const products = [
            {
                    id: 1,
                    name: 'Pepper Spray',
                    price: 25,
                    description: 'Effective pepper spray for women safety',
                    image: 'https://m.media-amazon.com/images/I/61kqjJ9kCoL.jpg'
                },
                {
                    id: 2,
                    name: 'Safety Alarm',
                    price: 15,
                    description: 'Compact alarm to alert others in emergencies',
                    image: 'https://m.media-amazon.com/images/I/41A14UAEsJL.jpg'
                },
                {
                    id: 3,
                    name: 'Self-defense Keychain',
                    price: 10,
                    description: 'Portable self-defense tool in the form of a keychain',
                    image: 'https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcTBW2cr-S9JMF6M7LYV9vdsL6POxricAmXUyQ&s'
                },
                {
                    id: 4,
                    name: 'Self-defense Safety Stick',
                    price: 5,
                    description: 'This is a self-defense tool made from durable materials like stainless steel. It is adjustable in length and can be used for self-defense in various situations',
                    image: 'https://www.trpmart.com/wp-content/uploads/2018/06/26-Inches-Branded-Baton-600x600.jpg'
                },
                {
                    id: 5,
                    name: 'Lanyard Self-defence Gadgets',
                    price: 6,
                    description: 'The innovative dual hole design allows for flexible usage',
                    image: 'https://i.ytimg.com/vi/Yv5uJqe8vCI/maxresdefault.jpg'
                },
                {
                    id: 6,
                    name: 'Pocket Knife Keychains',
                    price: 4,
                    description: 'The portable mini knife is small in size, light in weight, easy for you to suspend it on your waist belt or backpack belt, can use it anytime anywhere for unexpected needs.',
                    image: 'https://i5.walmartimages.com/asr/94c01083-e0ed-438f-845f-760368c9d0e1_1.f0653b805ef14e6cbb81610cd061ae97.jpeg'
                },
                
            ];

            // Display product data
            const productListElement = document.getElementById('product-list');
            products.forEach(product => {
                const productCard = document.createElement('div');
                productCard.classList.add('product-card');

                productCard.innerHTML = `
                    <div>
                        <h3>${product.name}</h3>
                        <p>${product.description}</p>
                        <p><strong>Price:</strong> $${product.price}</p>
                        <button onclick="purchaseProduct(${product.id})">Purchase</button>
                        <button onclick="payWithPolygon(${product.price})">Pay with Polygon</button>
                    </div>
                    <img src="${product.image}" alt="${product.name}" />
                `;

                productListElement.appendChild(productCard);
            });
        });

        // Function to simulate product purchase
        function purchaseProduct(productId) {
            alert(`Product with ID ${productId} purchased successfully!`);
            window.location.href = `/purchase/${productId}`;


        }

        // Function to handle payment with Polygon using MetaMask
        async function payWithPolygon(price) {
            // Check if MetaMask is installed
            if (typeof window.ethereum === 'undefined') {
                alert('Please install MetaMask to proceed with payment.');
                return;
            }

            try {
                // Request account access if needed
                await window.ethereum.request({ method: 'eth_requestAccounts' });

                // Get the current user's address
                const accounts = await window.ethereum.request({ method: 'eth_accounts' });
                const senderAddress = accounts[0];

                // Define the transaction parameters
                const transactionParameters = {
                    to: '0xRecipientAddress', // Replace with the recipient's address
                    from: senderAddress, // Current user's address
                    value: (price * Math.pow(10, 18)).toString(16), // Convert price to wei
                    gas: '21000', // Gas limit
                };

                // Send the transaction
                const txHash = await window.ethereum.request({
                    method: 'eth_sendTransaction',
                    params: [transactionParameters],
                });

                alert(`Transaction sent! Hash: ${txHash}`);
            } catch (error) {
                console.error(error);
                alert('Transaction failed. Please try again.');
            }
        }
    </script>
</body>
</html>

